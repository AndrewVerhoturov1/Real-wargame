name: Per-unit AI scheduler smoke and Combat Foundation Core

on:
  workflow_dispatch:

permissions:
  contents: read

# Documentation-only ownership markers retained for the scheduler contract smoke.
# The lightweight PR path classification itself is centralized in PR Risk CI.
env:
  AI_SCHEDULER_CONTRACT_PATHS: >-
    'src/core/ai/AiSimulationScheduler.ts'
    'src/core/ai/AiGameBridge.ts'
    'src/core/ai/AiStatefulMoveGameBridge.ts'
    'src/core/simulation/SimulationTick.ts'
    'src/core/simulation/SimulationState.ts'
    'src/core/behavior/BehaviorModel.ts'
    'src/core/units/UnitModel.ts'
    'scripts/ai_per_unit_scheduler_smoke.mjs'
    'scripts/ai_per_unit_scheduler_smoke.ts'
    'package.json'
  AI_SCHEDULER_EVIDENCE_FILE: ai-scheduler-smoke.log

jobs:
  combat-foundation:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run preserved combat integration matrix
        run: |
          npm run shared-visibility-vegetation:smoke
          npm run danger-route-cost-parity:smoke
          npm run combat-foundation:smoke
          npm run perception:smoke
          npm run perception-performance:smoke
          npm run view-memory-heatmap:smoke
          npm run view-memory-heatmap-performance:smoke
          npm run visibility-probe:smoke
          npm run runtime:smoke
          npm run ai-scheduler:smoke
          npm run workspace:smoke
          npm run awareness-field:smoke
          npm run directional-terrain:smoke
          npm run reverse-slope-comparative:smoke
          npm run navigation-overlay:smoke
          npm run pathfinding:smoke
          npm run navigation-profiles:smoke
          npm run routed-move:smoke
          npm run route-status:smoke
          npm run move-bridge:smoke
          npm run danger-layer-performance:smoke
          npm run danger-layer-movement-performance:smoke
          npm run combat-tactical-integration:smoke
          npm run lab:smoke
      - run: npm run build
      - run: npm run docs:check
