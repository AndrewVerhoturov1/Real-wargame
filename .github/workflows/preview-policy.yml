name: Preview Policy

on:
  pull_request:
    types: [opened, synchronize, reopened, edited, ready_for_review]

permissions:
  contents: write

jobs:
  check-preview-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Check preview policy
        env:
          PR_BODY: ${{ github.event.pull_request.body || '' }}
        shell: bash
        run: |
          set -e
          BASE_BRANCH="${{ github.base_ref }}"
          HEAD_BRANCH="${{ github.head_ref }}"
          echo "=== Preview Policy Check ==="
          echo "Base branch: $BASE_BRANCH"
          echo "Head branch: $HEAD_BRANCH"
          DEFAULT_TARGET="real-wargame-preview"
          if [ "$BASE_BRANCH" = "main" ]; then
            if echo "$HEAD_BRANCH" | grep -qE '^(codex/|preview/|r-init/)'; then
              if printf '%s\n' "$PR_BODY" | grep -Eiq 'MAIN_GO_APPROVED_BY_USER:[[:space:]]*yes'; then
                echo "preview_policy=pass" >> $GITHUB_ENV
                exit 0
              else
                echo "preview_policy=fail" >> $GITHUB_ENV
                exit 1
              fi
            else
              echo "preview_policy=pass" >> $GITHUB_ENV
            fi
          else
            echo "preview_policy=pass" >> $GITHUB_ENV
          fi

      - name: Print policy result
        shell: bash
        run: echo "Preview policy result: ${{ env.preview_policy }}"

  prepare-combat-integration:
    if: github.head_ref == 'tmp/prepare-combat-integration-run-20260714'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout integration runner
        uses: actions/checkout@v4
        with:
          ref: tmp/prepare-combat-integration-run-20260714
          fetch-depth: 0

      - name: Prepare and verify transfer branch
        shell: bash
        run: |
          set +e
          bash scripts/prepare_combat_integration.sh > integration.log 2>&1
          status=$?
          tail -n 160 integration.log
          exit $status
