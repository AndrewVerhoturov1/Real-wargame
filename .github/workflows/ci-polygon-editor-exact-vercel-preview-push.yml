name: Polygon Editor Exact Vercel Preview Push

on:
  push:
    branches:
      - ci/polygon-editor-exact-deploy-push-20260817-0792cae

permissions:
  contents: read

concurrency:
  group: polygon-editor-exact-preview-push-0792cae
  cancel-in-progress: false

jobs:
  preview:
    name: Verify and publish exact Polygon editor Preview
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      REQUESTED_REF: feature/20260817-polygon-editor-inner-parity
      EXPECTED_SHA: 0792cae6ba353c781847d3e2f7f588cdf7047329
      EXPECTED_VERCEL_PROJECT_NAME: repo
    steps:
      - name: Checkout exact product SHA
        uses: actions/checkout@v4
        with:
          ref: 0792cae6ba353c781847d3e2f7f588cdf7047329
          fetch-depth: 0

      - name: Verify exact source before installation
        id: source
        shell: bash
        run: |
          set -euo pipefail
          actual_sha="$(git rev-parse HEAD)"
          echo "Requested ref: $REQUESTED_REF"
          echo "Checked out SHA: $actual_sha"
          test "$actual_sha" = "$EXPECTED_SHA"
          echo "ref=$REQUESTED_REF" >> "$GITHUB_OUTPUT"
          echo "sha=$actual_sha" >> "$GITHUB_OUTPUT"

      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install exact dependencies
        run: npm ci --no-audit --no-fund

      - name: Run canonical Preview gate
        id: gate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/deployment
          report="$GITHUB_WORKSPACE/artifacts/deployment/preview-verification.json"
          npm run verify:preview -- --report "$report"
          echo "report=$report" >> "$GITHUB_OUTPUT"

      - name: Pull permanent Vercel project settings
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          set -euo pipefail
          : "${VERCEL_TOKEN:?VERCEL_TOKEN is missing}"
          : "${VERCEL_ORG_ID:?VERCEL_ORG_ID is missing}"
          : "${VERCEL_PROJECT_ID:?VERCEL_PROJECT_ID is missing}"
          npx --yes vercel@56.4.1 pull --yes --environment=preview --token="$VERCEL_TOKEN"

      - name: Verify permanent Vercel project link
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: >-
          node scripts/verify_vercel_project_link.mjs
          --file .vercel/project.json
          --expected-org-id "$VERCEL_ORG_ID"
          --expected-project-id "$VERCEL_PROJECT_ID"
          --expected-project-name "$EXPECTED_VERCEL_PROJECT_NAME"

      - name: Build Vercel output once
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          REAL_WARGAME_BRANCH: ${{ steps.source.outputs.ref }}
          REAL_WARGAME_COMMIT_SHA: ${{ steps.source.outputs.sha }}
        run: npx --yes vercel@56.4.1 build --token="$VERCEL_TOKEN"

      - name: Add exact deployment identity
        run: >-
          node scripts/write_deployment_source.mjs
          --root .vercel/output/static
          --ref "${{ steps.source.outputs.ref }}"
          --sha "${{ steps.source.outputs.sha }}"
          --report "${{ steps.gate.outputs.report }}"

      - name: Verify prebuilt output pages
        run: npm run verify:deployment-pages -- --root .vercel/output/static --require-source

      - name: Publish one prebuilt Preview deployment
        id: deploy
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          set -euo pipefail
          deployment_url="$(npx --yes vercel@56.4.1 deploy --prebuilt --yes --token="$VERCEL_TOKEN")"
          deployment_url="$(printf '%s\n' "$deployment_url" | tail -n 1 | tr -d '\r')"
          [[ "$deployment_url" =~ ^https://[^[:space:]]+$ ]]
          echo "url=$deployment_url" >> "$GITHUB_OUTPUT"
          echo "Preview URL: $deployment_url"

      - name: Wait for READY
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: npx --yes vercel@56.4.1 inspect "${{ steps.deploy.outputs.url }}" --wait --token="$VERCEL_TOKEN"

      - name: Verify published pages and identity
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.url }}
          EXPECTED_REF: ${{ steps.source.outputs.ref }}
          EXPECTED_SHA: ${{ steps.source.outputs.sha }}
        run: |
          set -euo pipefail
          npx --yes vercel@56.4.1 curl / --deployment "$DEPLOYMENT_URL" --token="$VERCEL_TOKEN" > /dev/null
          npx --yes vercel@56.4.1 curl /combat-lab.html --deployment "$DEPLOYMENT_URL" --token="$VERCEL_TOKEN" > /dev/null
          npx --yes vercel@56.4.1 curl /ai-node-editor.html --deployment "$DEPLOYMENT_URL" --token="$VERCEL_TOKEN" > /dev/null
          source_file="$RUNNER_TEMP/published-deployment-source.json"
          npx --yes vercel@56.4.1 curl /deployment-source.json --deployment "$DEPLOYMENT_URL" --token="$VERCEL_TOKEN" > "$source_file"
          SOURCE_FILE="$source_file" node <<'NODE'
          const fs = require('node:fs');
          const source = JSON.parse(fs.readFileSync(process.env.SOURCE_FILE, 'utf8'));
          if (source.ref !== process.env.EXPECTED_REF || source.sourceSha !== process.env.EXPECTED_SHA) {
            throw new Error(`Deployment identity mismatch: ${source.ref} @ ${source.sourceSha}`);
          }
          if (source.verificationStatus !== 'passed' || (source.skippedChecks ?? []).length !== 0) {
            throw new Error('Deployment verification record is not fully passed.');
          }
          console.log(`Verified published source: ${source.ref} @ ${source.sourceSha}`);
          NODE

      - name: Write deployment evidence
        shell: bash
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.url }}
        run: |
          mkdir -p artifacts/deployment
          node <<'NODE'
          const fs = require('node:fs');
          fs.writeFileSync('artifacts/deployment/result.json', JSON.stringify({
            url: process.env.DEPLOYMENT_URL,
            ref: process.env.REQUESTED_REF,
            sha: process.env.EXPECTED_SHA,
            status: 'READY'
          }, null, 2));
          NODE

      - name: Upload deployment evidence
        uses: actions/upload-artifact@v4
        with:
          name: polygon-editor-exact-preview-${{ github.run_id }}
          path: artifacts/deployment/**
          retention-days: 14
