# Temporary visual QA trigger

This branch exists only to trigger the real GitHub Actions screenshot workflow for the human DangerAbove and StressAbove node UI.

Do not merge this file into `real-wargame-preview` unless the user explicitly asks for it.
