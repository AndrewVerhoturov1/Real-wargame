# Temporary visual QA trigger

This branch exists only to trigger the real GitHub Actions screenshot workflow for the universal `BlackboardValueAbove` node UI after tooltip hover fix.

Do not merge this file into `real-wargame-preview` unless the user explicitly asks for it.
