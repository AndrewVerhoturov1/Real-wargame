# Temporary visual smoke check

Purpose: trigger Preview screenshots after UI feedback fixes: mode/editor button integration, fixed unit card, no view cones, layers tab with known unit information, and tabbed editor workbench.

This branch must not be merged. Close the PR after screenshot inspection.
