# Soldier Top-Down Appearance

## Goal

Выбрать внешний вид солдат в виде сверху: сравнить варианты силуэта, читаемости, цветов сторон, типов солдат, оружия и состояний, и зафиксировать выбор.

## Current focus

Исследование вариантов внешнего вида солдат сверху. Внешний вид ещё не выбран — сравнить силуэт, читаемость, цвета сторон, типы солдат, оружие и состояния.

## Key decisions

- Внешний вид солдат сверху пока не выбран; подпроект в статусе planned.

## Read first

1. `docs/subprojects/soldier-topdown-appearance/SUBPROJECT.md`
2. `docs/subprojects/soldier-topdown-appearance/subproject.json`
3. `docs/subprojects/soldier-topdown-appearance/JOURNAL.md` (если существует)
4. `python scripts/subproject_context.py soldier-topdown-appearance --brief`

## Boundaries

- Read only files needed for the current focus.
- Do not scan raw telemetry or report roots by default.

## Testing

Test program: `docs/subprojects/soldier-topdown-appearance/test-program.md` (создать при появлении тестов)
