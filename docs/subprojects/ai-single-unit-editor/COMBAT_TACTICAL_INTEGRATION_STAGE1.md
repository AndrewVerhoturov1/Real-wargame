# Этап 1 интеграции реального боя с тактическими системами

## Статус

Исходный код этапа перенесён в `real-wargame-preview` через PR #102 (`c885a46d1549632494bddac52ee2583b176a016c`). Slice 1 памяти угроз перенесён через PR #106; проверенный preview SHA — `3f01f4ba9b96daa1b8951bdd08f4005a482fee8c`.

По состоянию на 2026-07-15 этап **не закрыт**. Slice 1 закрыл разделение danger/suppression и строгую память неизвестного огня, но остаются live route replan, усиленные safe-position/reverse-slope доказательства, постоянный CI и выполненная visual QA.

Проверенный preview baseline после Slice 1: `3f01f4ba9b96daa1b8951bdd08f4005a482fee8c`. Gate 0 baseline `d8d9c4e910a2c4246bb56893dd5a3dd081125fa0` остаётся исторической точкой аудита до Slice 1.

## Gate 0: фактический baseline preview

| Уже находится в `real-wargame-preview` | Доказательство |
|---|---|
| Hierarchical State/Plan v1 (`Idle`, `FollowingOrder`, `Contact`, `Suppressed`; планы `FollowMoveOrder`, `TakeCover`) | интеграционный коммит `0c2b335f933833cfdb8e23eada8aeff89937435d`; проходят `state-machine:smoke`, `plan-runtime:smoke`, `state-plan-scenario:smoke` |
| Tactical Query System cover v1 (`create → filter → score → select`) | коммит `3ae67e8`; проходит `tactical-query:smoke` |
| Combat Tactical Integration Stage 1 | PR #102, merge-коммит `c885a46d1549632494bddac52ee2583b176a016c`; проходит существующий `combat-tactical-integration:smoke` |
| Нодный контракт будущего боевого поведения | зафиксирован в [Graph v2: типизированные контракты и подграфы](GRAPH_V2_TYPED_CONTRACTS_AND_SUBGRAPHS.md) и в планах кампании |

### Что действительно осталось завершить в Stage 1

| № | Активный gap | Критерий закрытия |
|---|---|---|
| 1 | ✅ Выполнено в Slice 1: danger отделён от suppression | обычный `unit:<id>` сохраняет потенциальную опасность при `suppression = 0`; evidence-derived suppression/stress и pressure zones живут и затухают отдельно |
| 2 | ✅ Выполнено в Slice 1: неизвестный огонь имеет устойчивую память | согласованные события объединяются по направлению, времени и области неопределённости; разные направления остаются отдельными; совместимая память reconciles с `unit:<id>` без утечки скрытой позиции |
| 3 | Нет полноценного сквозного доказательства живого перестроения маршрута | настоящий контакт/выстрел меняет revision, обычный `SimulationTick` перестраивает активный order, сохраняет owner/goal/profile/final facing, а гистерезис не допускает replan каждый tick |
| 4 | Тест безопасных позиций не доказывает смену победителя | лучший кандидат меняется после угрозы и находится на защищённой стороне стены |
| 5 | Влияние обратного склона не доказано сравнением | flat/reverse-slope и смена направления угрозы предсказуемо меняют оценку позиции и маршрут |
| 6 | Smoke этапа не закреплён в постоянном CI доставки preview | `combat-tactical-integration:smoke` становится обязательным шагом соответствующего workflow, визуальные тесты остаются manual-only |
| 7 | Подготовленная визуальная проверка не запускалась | после отдельного разрешения проверены реальное приложение, точный SHA и свежие PNG девяти сцен |
| 8 | Финальное закрытие документации ещё впереди | статус меняется на «завершено» только после прохождения пунктов 1–7 |

Подробный источник критериев: [план завершения Stage 1](../../../plans/2026-07-14-combat-tactical-integration-stage1-followup.md).

## Slice 1: danger отдельно от evidence-derived suppression

Slice 1 находится в `real-wargame-preview` через PR #106 на SHA `3f01f4ba9b96daa1b8951bdd08f4005a482fee8c`.

Выполнено:

- обычный визуальный, звуковой или reported контакт сохраняет `strength`, confidence, сектор, дальность и потенциальную danger, но не создаёт suppression сам по себе;
- suppression и `stressPerSecond` появляются от свежих `CombatThreatEvidence` либо существующих `pressureZones`;
- evidence-состояние известной угрозы переживает обычное обновление контакта и независимо затухает;
- повторный неизвестный огонь объединяется по времени, близости направления и пересечению областей неопределённости с устойчивым threat id и bounded strength/suppression;
- сильно различающиеся направления не склеиваются, включая два события до одного sync с одинаковым непустым `sourceUnitId`;
- после обнаружения стрелка совместимая `unknown-fire:*` память переносится в `unit:<id>` без копирования скрытой объективной позиции;
- `evidenceCount` и `lastEvidenceSeconds` сохраняются через scene persistence, а legacy-сцены нормализуются безопасно.

Техническая проверка без браузера выполнена в GitHub Actions run `29365567387`: прошли `combat-tactical-integration:smoke`, отдельный source-direction regression, `combat-foundation:smoke`, `perception:smoke`, `state-machine:smoke`, `graph-v2:smoke`, `runtime:smoke`, `validate:ai-graph` и production build.

Browser, Playwright и PNG для Slice 1 не запускались; visual QA остаётся manual-only.

## Контракт нодового поведения для следующих срезов

Полный боевой цикл не должен сначала создаваться как отдельная code-first политика с последующим переносом в редактор. Код предоставляет детерминированные факты, расчёты, persistence и неотключаемые проверки безопасности; пороги, веса, задержки, гистерезис, Utility-выбор и композиция реакций принадлежат Graph v2 и подграфам.

Настраиваемое поведенческое значение считается готовым только когда оно доступно в свойствах ноды, сериализуется и восстанавливается, имеет русские подписи и причины и видно в trace без редактирования JSON или TypeScript. Канонические подробности находятся в [документе Graph v2](GRAPH_V2_TYPED_CONTRACTS_AND_SUBGRAPHS.md) и [дорожной карте Stage 2–7](../../../plans/COMBAT_TACTICAL_INTEGRATION_REMAINING_WORK.md).

## Проверки Gate 0

На изолированном worktree от audited code baseline выполнены без браузера:

```text
npm run state-machine:smoke
npm run plan-runtime:smoke
npm run state-plan-scenario:smoke
npm run tactical-query:smoke
npm run combat-tactical-integration:smoke
npm run docs:sync
npm run docs:check
npm run docs:smoke
npm run build
```

Visual QA в Gate 0 не запускалась и не считается выполненной.

## Что соединено

Новая цепочка данных не заменяет существующие системы и не создаёт вторую карту опасности:

```text
реальный вражеский юнит
→ существующий PerceptionContactMemory (`unit:<id>`)
→ syncSoldierThreatMemory
→ UnitTacticalKnowledge.threats
→ SoldierAwarenessGrid
→ DirectionalTacticalField
→ безопасные позиции / рельеф / RouteCostField / A*
```

Для выстрела цепочка выглядит так:

```text
BallisticRaycast
→ ограниченная проверка бойцов рядом с отрезком траектории
→ кратковременное CombatSuppression
→ CombatThreatEvidence
→ syncSoldierThreatMemory
→ те же существующие тактические поля
```

`pressureZones` продолжают использовать прежние идентификаторы, геометрию, настройки и физическую оценку давления.

## Реальный контакт как субъективная угроза

`syncSoldierThreatMemory` теперь понимает не только контакты `threat:<zone>`, но и контакты `unit:<id>`.

Для реального противника используются только данные личного контакта наблюдателя:

- `lastKnownPosition`;
- уверенность;
- неопределённость;
- стадия контакта;
- источник (`visual`, `sound`, `reported`, `fire_pressure`);
- `visibleNow`;
- время последнего наблюдения.

Из реального объекта противника разрешено прочитать только сторону для проверки враждебности и подпись. Текущая позиция юнита не используется для построения угрозы.

Если цель потеряна:

- остаётся последнее известное положение;
- уверенность уменьшается;
- неопределённость растёт;
- позиция не обновляется вслед за скрытым юнитом;
- слабая старая память удаляется.

Звуковой контакт получает значительно большую неопределённость, чем визуальный.

## Реальные пули и подавление

`CombatSuppression` оценивает только бойцов в ограниченной области вокруг фактического отрезка траектории и точки попадания.

Величина воздействия зависит от:

- расстояния до траектории;
- расстояния до попадания;
- прямого попадания;
- начальной скорости пули как простого коэффициента мощности оружия;
- числа недавних выстрелов;
- существующей оценки укрытия;
- позы;
- стойкости бойца;
- типа попадания.

Модель детерминирована. Далёкий пролёт отбрасывается. Близкий пролёт, попадание рядом и прямое попадание дают последовательно более сильную реакцию.

Кратковременное подавление хранится отдельно от долговременной памяти и быстро затухает. История недавних выстрелов и свидетельств имеет жёсткие ограничения размера и времени жизни.

## Приблизительное направление неизвестного огня

Близкий пролёт, попадание рядом или ранение создают `CombatThreatEvidence`.

Свидетельство содержит:

- направление прилёта;
- примерную область источника;
- уверенность;
- неопределённость;
- сектор и предполагаемую дальность;
- силу и подавление;
- время и число согласующихся событий.

Примерная область строится из направления прилёта с детерминированной ошибкой. Реальные координаты стрелка в неё не копируются.

Если личный контакт со стрелком уже существует, свидетельство усиливает эту угрозу. Если контакта нет, создаётся устойчивый `unknown-fire:*`. Совместимые события объединяются по направлению, времени и пересечению областей неопределённости; несовместимые направления остаются отдельными даже при одинаковом `sourceUnitId`. После появления подходящего контакта evidence reconciles с `unit:<id>`, а неизвестный дубликат удаляется без чтения скрытой текущей позиции.

## Рельеф, укрытия и безопасные позиции

Новые угрозы не имеют отдельного поля. Они автоматически попадают в уже существующие:

- `SoldierAwarenessGrid`;
- `DirectionalTacticalField`;
- оценку укрытия;
- оценку обратного и прямого склона;
- риск гребня и силуэта;
- `bestSafePositions`.

Физическое воздействие конкретной пули использует `evaluateSmallArmsCover` только для ограниченного списка потенциально затронутых бойцов. Полный LOS по всем клеткам не запускается.

Стена, складка или обратный склон уменьшают воздействие. Попадание в объект рядом всё ещё может дать небольшой стресс и информацию о направлении.

## Маршруты и перестроение

`RouteCostField` уже принимает `UnitTacticalKnowledge.threats`, поэтому реальные контакты и неизвестный огонь изменяют:

- стоимость опасности;
- направленную стоимость рельефа;
- предпочтение обратных склонов;
- штраф гребня и силуэта;
- маршрут A*.

Политика перестроения не заменена. Продолжают действовать:

- минимальный интервал ревизий опасности;
- задержка между перестроениями;
- минимальное улучшение стоимости;
- отдельное правило заблокированного маршрута.

Отпечаток тактической памяти использует крупные пороги уверенности, неопределённости, направления и силы. Небольшое затухание не создаёт новую ревизию каждый кадр.

## Производительность

- Бойцы индексируются пространственными корзинами размером 8 клеток.
- Индекс переиспользуется всеми выстрелами текущего такта.
- Для выстрела проверяются только корзины расширенного прямоугольника траектории.
- Полная карта опасности не пересчитывается на каждый выстрел.
- Нет отдельных графических объектов на клетки.
- Нет LOS по каждой точке траектории и каждому бойцу.
- Свидетельств не больше 24 на бойца, время жизни — 12 секунд.
- История недавних выстрелов ограничена 16 событиями и четырьмя секундами.

## Автоматическая проверка этапа

`npm run combat-tactical-integration:smoke` выполняет 17 проверок:

1. Реальный контакт становится угрозой с danger, но без contact-derived suppression.
2. Скрытое движение не раскрывает позицию.
3. Близкий пролёт создаёт suppression и примерное направление.
4. Далёкий пролёт почти не влияет.
5. Укрытие уменьшает suppression.
6. Реальная угроза меняет безопасные позиции.
7. Реальная угроза меняет стоимость маршрута и допускает replan policy.
8. Память и evidence-состояние затухают и удаляются.
9. Звук, огонь и визуальный контакт не создают дубликаты известного стрелка.
10. Persistence восстанавливает субъективную угрозу без подмешивания скрытой позиции.
11. Evidence suppression переживает refresh контакта и монотонно затухает.
12. Согласованный неизвестный огонь объединяется между тактами с устойчивым id.
13. События по разные стороны старой bucket-границы корректно объединяются.
14. Сильно различающиеся направления остаются отдельными угрозами.
15. Обнаруженный стрелок получает evidence alias без неизвестного дубликата и утечки скрытых данных.
16. Новые evidence-поля проходят round-trip и legacy normalization.
17. Сценарные pressure zones остаются независимым источником suppression.

Отдельный `combat_threat_evidence_source_direction_regression_smoke.ts`, подключённый к тому же npm smoke, фиксирует P2: два события до одного sync, одинаковый непустой `sourceUnitId`, направления `10°` и `190°` должны создать две разные `unknown-fire:*` угрозы.

Независимая техническая проверка run `29365567387` прошла полностью. Постоянный workflow доставки preview пока не запускает этот smoke как обязательный шаг, поэтому CI-gap Stage 1 остаётся открытым.

## Подготовленная визуальная проверка

Режим запуска:

```text
/?visualQa=combat-tactical-integration
```

API проверки:

```text
window.__realWargameCombatTacticalVisualQa
```

Подготовлены девять сцен:

- `visual-contact`;
- `near-miss`;
- `wall-cover`;
- `reverse-slope`;
- `slice1-contact-danger-zero-suppression`;
- `slice1-near-miss-evidence-suppression`;
- `slice1-wall-evidence-attenuation`;
- `slice1-repeated-unknown-fire-merged`;
- `slice1-detected-shooter-alias`.

Playwright-файл: `tests/combat-tactical-integration.spec.ts`.

Для пяти Slice 1 сцен подготовлены PNG:

- `combat-stage1-slice1-01-contact-danger-zero-suppression.png`;
- `combat-stage1-slice1-02-near-miss-evidence-suppression.png`;
- `combat-stage1-slice1-03-wall-evidence-attenuation.png`;
- `combat-stage1-slice1-04-repeated-unknown-fire-merged.png`;
- `combat-stage1-slice1-05-detected-shooter-alias.png`.

Фактический browser/Playwright запуск выполняется только после отдельного согласия пользователя. На SHA `3f01f4b` он не выполнялся, PNG не создавались и визуальная приёмка не считается пройденной.

## Ограничения этапа 1

Пока не сделаны:

- решение о стрельбе через Utility AI;
- автоматические очереди;
- броня, пробитие и рикошеты;
- радио и передача контактов между бойцами;
- медицинская система;
- полноценный базовый граф солдата;
- новые ноды угроз;
- сложная физика ударной волны и акустики.

## Что относится к этапу 2

Следующий этап может соединить уже готовую тактическую информацию с решениями поведения:

- выбор реакции на подавление;
- выбор укрытия и позы;
- решение о подавляющем огне;
- приоритет цели;
- прекращение, перенос и возобновление огня;
- выполнение этих решений через состояния, планы и переиспользуемые подграфы.
