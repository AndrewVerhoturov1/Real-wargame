# Subproject Journal

## Rules

- Record important and medium-importance steps only, not every micro-step.
- Keep entries short: what changed, why, and the result.
- Do not paste raw logs, telemetry, or full reports.
- One entry per significant event or decision.

## Entries

- **2026-07-09**: Created `ai-single-unit-editor` subproject as a draft scaffold from `_template/`. Goal: node-based AI constructor for single-unit behavior, tied to existing RTS wrapper (Real-wargame). Key decisions: not a generic framework, single-unit only first, works through existing BehaviorModel/UnitModel/SimulationState. Boundaries recorded against rewriting the full RTS simulation, premature squad AI, and detached node framework. SUBPROJECT.md, subproject.json, JOURNAL.md created and verified via `subproject_context.py` --list, --brief, --opencode, --files.
- **2026-07-09**: Added `LOCAL_ENGINE_NODE_EDITOR_IMPLEMENTATION_PLAN.md`. Clarified that AI Node Editor must open in a new browser tab/entrypoint, while heavy AI calculations run in a local engine process rather than the browser tab. Recorded phased implementation: JSON graph data contract, headless local engine, engine-backed validation/evaluate-once, Soldier Survival Brain v0.1, tactical queries, recipes, worker_threads, and optional Rust/Tauri sidecar only after profiling. Updated `subproject.json` with the new plan, planned files, local-engine safety rules, and verification notes.
- **2026-07-09**: Implemented step 1 data contract on `real-wargame-preview`: added `src/core/ai/AiGraph.ts`, `AiNodeTypes.ts`, `AiBlackboard.ts`, `AiGraphValidation.ts`, bundled `src/data/ai/soldier_default_survival_graph.json`, and `scripts/validate_ai_graph.mjs` with npm script `validate:ai-graph`. This establishes a headless JSON contract for a single-soldier Soldier Survival Brain before visual editor or local engine server work. Updated SUBPROJECT.md and subproject.json with the new files and verification commands.
- **2026-07-09**: Implemented step 2 headless local AI engine. Added shared JS engine core, local HTTP server, smoke script, npm scripts `engine:dev`/`engine:smoke`, root launchers `Run-AI-Engine.bat` and `Run-AI-Engine-Smoke.bat`, plus `docs/manual-test/AI_ENGINE_STAGE_2.md`. Endpoints: `GET /engine/health`, `POST /ai/graph/validate`, `POST /ai/graph/evaluate-once`. Stage 2 remains headless: no visual node editor, no SimulationTick control, no live map movement yet.
- **2026-07-09**: Implemented step 3 visible AI Node Editor. Added `ai-node-editor.html`, `src/ai-node-editor/main.ts`, `src/ai-node-editor/ai-node-editor.css`, static smoke `scripts/ai_node_editor_smoke.mjs`, npm script `editor:smoke`, launcher `Run-AI-Node-Editor.bat`, and manual guide `docs/manual-test/AI_NODE_EDITOR_STAGE_3.md`. Tactical map now has `Редактор ИИ` button that opens the editor in a new tab. Stage 3 remains read-only/preview: no node drag/save yet and no live SimulationTick control.
- **2026-07-09**: Simplified manual check for unclear editor points 4–5. Added top-bar button `Автопроверка 4–5` that checks `/engine/health`, `/ai/graph/validate`, `/ai/graph/evaluate-once` and prints plain Russian lines `Пункт 4 OK` / `Пункт 5 OK` instead of requiring the user to inspect raw JSON. Updated smoke and manual guide accordingly.
- **2026-07-09**: Migrated AI graph/engine contract toward English base + Russian overlay. Added English `label`/`description`/`displayName`/`reason`/`explanation` fields while keeping `*Ru` fields for Russian UI. Updated node type definitions, blackboard schema, graph contract, bundled graph, validation result shape, local engine responses, and editor smoke checks. Goal: reduce Cyrillic in logic/data contracts while preserving Russian readable layer for the user.
- **2026-07-09**: Implemented step 4 authoring in AI Node Editor. The editor can now add nodes from the palette, drag nodes, link selected parent to child, remove links, edit English/Russian node text and parameters JSON, delete non-root nodes, persist working graph/positions in localStorage, export/import JSON, reset to bundled graph, and validate/evaluate the changed graph through local engine. Added authoring CSS and `docs/manual-test/AI_NODE_EDITOR_STAGE_4.md`. Still not connected to live SimulationTick.
- **2026-07-09**: Replaced separate threshold condition node types with universal `BlackboardValueAbove`. The bundled danger/stress checks now use the same node type with different `parameters.sourceKey` (`danger` / `stress`) and `parameters.threshold`. Human UI now shows a parameter selector + threshold controls instead of separate `DangerAbove`/`StressAbove` panels. Local engine reads thresholds from graph nodes rather than hard-coded per-type condition nodes.
- **2026-07-09**: Extended universal `BlackboardValueAbove` with `parameters.comparison` (`above` / `below`). The same node can now express “parameter above threshold” and “parameter below threshold” through human UI buttons, while local engine validates and evaluates both modes. Bundled danger/stress conditions remain `comparison: above`.
- **2026-07-09**: Added simple universal node catalog and human panels: `Числовой порог`, `Проверка флага`, `Порог расстояния`, `Оценка параметра`, `Оценка расстояния`, `Поиск объекта`, `Запись памяти`, `Копия памяти`, `Действие`, `Режим движения`, `Реплика бойца`, `Стабильный порог`, `Запрет действия`, and related target/chance/inertia nodes. Added common per-node cooldown UI (`cooldownSeconds`, `cooldownTiming: before/after`) and local engine validation for cooldowns and `SayMessage` text. Screenshots intentionally skipped by user request.
- **2026-07-09**: Cleaned AI Node Editor after legacy-node review. Removed replaceable one-off node types from palette/local engine (`HasOrder`, enemy/fire/cover checks, old score nodes, old cover/order/observe actions). Bundled graph now starts as a clean canvas with only `root`, and storage moved to `v6` to avoid resurrecting old localStorage graphs. Converted pseudo-parameters such as distance `self` into selector fields where choices are finite. Local engine evaluate-once now returns an honest preview result instead of hard-coded old survival branches.
- **2026-07-09**: Connected the universal AI graph to the actual tactical board as a first goal slice. Added `AiGameBridge.ts`, selected-unit blackboard building from game state, chainable universal node execution, before/after cooldowns, simple action/posture/movement/speech application, and HTML speech bubbles over units. The bridge reads the same `localStorage v6` graph as AI Node Editor and runs about every 0.6 seconds for the selected unit. Scoring/utility behavior is still a future full executor.
- **2026-07-09**: Added common quiet launch and shared mode menu. New `Run-Real-Wargame-Lab.bat` starts a hidden `real_wargame_lab_manager` on `127.0.0.1:8799`, Vite, and local AI engine, then opens `lab-launch.html` which opens game + AI Node Editor tabs. Shared `AppShellMenu` adds game buttons `Редактор ИИ солдат / Новая игра / Выход` and editor buttons `Обновить / Открыть игру / Выход`. Exit broadcasts tab-close intent and calls `/lab/shutdown` to stop local processes. Browser tab self-close is best-effort due browser restrictions.
- **2026-07-09**: Fixed human-panel select reset bug. Problem: choosing any `select` in the node editor immediately reset because document-level click handling triggered a rerender. Added `src/ai-node-editor/editor-click-guard.ts`, loaded before `main.ts`, to guard clicks inside `.human-node-panel`, `.inspector-panel`, `.app-shell-menu`, `select`, `input`, and `textarea` from document-click rerender.
- **2026-07-09**: Prepared next-chat handoff. Added `docs/subprojects/ai-single-unit-editor/HANDOFF.md`, refreshed `SUBPROJECT.md`, `subproject.json`, and manual Stage 4 test with current state, launch/menu behavior, live bridge scope, universal node rules, select-reset fix, boundaries, and next suggested tasks.
- **2026-07-09**: Added normal GraphRunner + UtilitySelector v1. New `src/core/ai/AiGraphRunner.ts` executes graph flow without PixiJS/SimulationState/localStorage, returns effects/scores/trace/explanation/cooldowns, and scores `UtilitySelector` branches through `ParameterScore`, `DistanceScore`, `DecisionInertia`, `RandomChance`, `StableThreshold`, and `ForbidAction`. `AiGameBridge.ts` now delegates thinking to the runner and only applies effects to the selected unit. Local engine evaluate-once and smoke were updated away from old `critical_survival` expectations; clean root-only graph now honestly returns `command.type=none`, while a separate smoke utility graph checks branch scoring.
- **2026-07-09**: Added runtime debug trace + pause without changing the core `SimulationState` contract. Game now has a runtime `paused` flag set from `src/main.ts`, HUD button `Пауза: вкл/выкл`, and hotkey `P`; `PixiApp` stops `tickSimulation` during pause while still rendering the map/HUD. `AiGameBridge` writes the latest GraphRunner trace to `real-wargame.ai-node-editor.debug.v1`. AI Node Editor loads `runtime-debug-overlay.ts/css`, reads that trace, highlights passed/failed/score/winner/veto nodes, and shows a small “След ИИ” panel with the selected unit, winning branch, explanation, and branch scores.
- **2026-07-10**: Implemented AI Test Lab Stage 5 in the tactical scene. Added editable fighter profiles/traits/condition/runtime values, realistic default speed `0.5 cell/s` (about 5 m/s at the current scale), time controls through `×10`, directional fire sectors with range/arc/falloff/visibility, combined danger and suppression, cover protection relative to fire direction, threat-aware safe cover points, preview-only and apply-now GraphRunner controls, fighter/scene reset, diagnostics, and scene export v3. Added new graph numeric inputs for threat distance/direction/arc and cover quality, machine smoke `lab:smoke`, non-visual preview core CI, design/implementation plan, and `AI_TEST_LAB_STAGE_5.md`. Visual browser verification remains separate and was not claimed.
- **2026-07-10**: Rebuilt the in-game map editor as the unified Stage 6 scene workbench. Replaced the installed legacy split panels with tabs `Предмет / Боец / Угроза / Рельеф / Сцена`; creation now uses fully configured drafts before map placement, and the same forms can copy/apply parameters for selected instances. Added object cover properties, complete fighter profiles/runtime values, directional threat setup, circular/square height and forest brushes, persistent scene tools, `game-editor:smoke`, CI coverage, design/plan, and `GAME_EDITOR_WORKBENCH_STAGE_6.md`. Existing selection/drag/resize/rotation and scene JSON remain compatible; real browser verification remains manual.
- **2026-07-10**: Implemented single-unit tactical awareness v1. Split fighter data into permanent traits, initial reset state and live runtime state; embedded the lab into the existing top/dock/bottom layout; added placement tools, selection priority by active tab and interactive threat gizmos. Added `SmallArmsCoverEvaluation`, individual `SoldierThreatMemory`, cached `SoldierAwarenessGrid`, Pixi threat/heatmap renderers and new blackboard values for current danger, expected protection, best safe position, route danger and threat confidence. Forest, relief and map objects now participate in small-arms protection/concealment estimates.
- **2026-07-10**: Completed real-browser QA for app commit `665a6a14d45fbce758daf86303155b4d538bff6b`. Core run `29086211637` passed; screenshot run `29086211662` passed; Playwright reported 3/3 scenarios and created 20 PNGs. The PNG artifact was downloaded and key frames were inspected. During QA fixed canvas coordinate offsets, active-tab selection priority, stale numeric fields after drag, unstable dock rerenders, awareness cache invalidation, system Chrome launch and the safe-map scale.
- **2026-07-10**: Refreshed `.agents/skills/real-wargame-local-preview/SKILL.md` with the verified workflow: primary lab launcher, system Chrome, mandatory Playwright log, SHA matching, real canvas coordinates, stable DOM diagnosis, current screenshot list and chat handoff format.
- **2026-07-10**: Rewrote the complete next-chat handoff and synchronized `SUBPROJECT.md`, `subproject.json` and `JOURNAL.md` with the actual tactical-awareness state, collaboration rules, verification evidence, limitations and next priorities.
- **2026-07-10**: Replaced the overlapping game/editor/AI-lab interface with Tactical Workspace Stage 7. The game side now has exactly two modes: `Симуляция` and `Редактирование`. Simulation received one collapsible right sidebar (`Инфо / Опасность / Скрытность / Память`), an RTS-style bottom unit bar, subjective danger and memory overlays, a dedicated concealment map, and interactive known-cover selection. Editing always pauses the simulation and reuses the unified scene workbench plus threat gizmos. The separate AI Test Lab launcher/UI is no longer installed; the AI Node Editor was not changed. Added `workspace:smoke`, Playwright coverage and `TACTICAL_WORKSPACE_STAGE_7.md`.
- **2026-07-11**: Added a deliberately minimal front-context model for the single-soldier AI: friendly territory, neutral zone and enemy territory are separated by two editable vertical X boundaries. Visibility is controlled from the main `Вид` menu; boundary sliders live only in the editor's `Сцена` tab; the soldier blackboard receives territory type flags and base safety values 80/50/20. The layer uses five fixed HTML elements and does not add per-cell Pixi rendering or automatic front movement.
- **2026-07-11**: Merged PR #50 into `real-wargame-preview` as `627a77ca4f98241ec863809d7e83f26bc1ba5b0c`. Post-merge core run `29152364956` and screenshot run `29152364936` passed; Playwright reported 7/7 and produced 14 PNG, including `14-simple-front-zones-editor.png`, which was inspected.
- **2026-07-11**: Added `ideas/FRONT_LINE_INFLUENCE_ON_SINGLE_SOLDIER_AI.md`. It defines territory as strategic context rather than enemy detection, separates `currentDanger` from `territorySafety`, and proposes first Utility AI effects: caution, cover seeking, observation and retreat toward friendly territory.
- **2026-07-11**: Rewrote HANDOFF and synchronized `SUBPROJECT.md`, `subproject.json` and `JOURNAL.md` with the implemented front-context model, verified commit/runs, runtime-only limitations and recommended next vertical slice.
- **2026-07-11**: Added the canonical Soldier AI Dictionary through PR #51 and preview commit `e7e3e60f3abbf2a114093e0e8089f71dab25242e`. The same English-base/Russian-overlay catalog now drives the game and node editor, Russian is default, live blackboard values and soldier traits are searchable, readiness is explicit, map focus is interactive, and preconfigured nodes can be created without manual JSON. Core build/smoke and Playwright 9/9 passed; screenshots `21` and `22` were inspected.
- **2026-07-11**: Completed the no-JSON authoring workbench through PR #52 and preview commit `4b22aac8be619a0837d75670210286fe920dedad`. Added safe `user_memory_N` creation, translated graph diagnostics for unknown/placeholder/simplified mechanics, and browser-local history of the last 20 AI decisions. Core build/smoke passed; Playwright reported 12/12 in 6.0 minutes; screenshots `23`–`25` were downloaded and inspected. `main` remained untouched.
