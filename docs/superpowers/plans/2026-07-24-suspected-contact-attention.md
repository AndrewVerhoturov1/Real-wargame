# План: внимание к подозрительному контакту

**База:** `real-wargame-preview` @ `efdd45cce30893ade212c6fe72efe46698109b13`

**Ветка:** `feature/20260724-suspected-contact-attention`

## 1. Зафиксировать ожидаемое поведение тестами

Изменить:

- `scripts/attention_ai_nodes_smoke.ts`;
- `scripts/ai_node_contract_ui_smoke.ts`.

Проверить:

- старый фиксированный сектор;
- вычисление направления по двум позициям Blackboard;
- безопасный провал без позиции;
- разворот только неподвижного AI-бойца;
- наличие новых настроек в редакторе.

## 2. Расширить существующую ноду

Изменить:

- `src/core/ai/AiGraphRunner.ts`;
- `src/core/perception/AttentionController.ts`.

Добавить совместимое вычисление угла по `self_position` и `suspected_enemy_position`. Не менять старый вычислитель и формат эффекта.

## 3. Добавить настройки в редактор

Изменить:

- `src/ai-node-editor/node-contract-ui.ts`.

Показать источник направления, ключ цели и ключ исходной позиции. Все игровые вычисления остаются в core.

## 4. Подготовить проверяемый граф временной ветки

Изменить:

- `src/data/ai/soldier_default_survival_graph.json`.

Собрать ветвь «подозрение → сектор по предполагаемой позиции» и запасную ветвь автоматического внимания.

## 5. Проверка готовности

Для удалённого режима выполнить профильную проверку через существующий `PR Risk CI`. Ожидаемая матрица включает TypeScript, проверки AI/внимания/редактора и производственную сборку. Тяжёлый замер производительности не нужен: изменение добавляет только постоянное число арифметических операций на решение и не меняет частоту восприятия.

## 6. Деплой

После зелёной проверки вручную запустить Vercel Preview для точного HEAD ветки. Подтвердить `READY`, главную страницу, редактор нод и `deployment-source.json` с точным ref/SHA. В основную ветку результат не переносить.